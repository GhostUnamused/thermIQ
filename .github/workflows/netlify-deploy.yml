name: Netlify Auto Deploy
on:
  push:
    branches:
      - main

jobs:
  trigger-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Netlify build hook
        run: curl -X POST -fsS "${{ secrets.NETLIFY_BUILD_HOOK }}"
